import React from 'react';

require('./Header.scss');

const Header = () => (
  <header className="app-header">
    <div className="logo">
      Boogaloo
    </div>
  </header>
);

export default Header;
